{
  "name": "alongside-challenge",
  "scripts": {
    "frontend:dev": "npm run dev -w @alongside/frontend",
    "frontend:build": "npm run build -w @alongside/frontend"
  },
  "workspaces": [
    "apps/*",
    "infrastructure",
    "packages/**"
  ],
  "engines": {
    "node": ">=20",
    "npm": ">=10"
  }
}
